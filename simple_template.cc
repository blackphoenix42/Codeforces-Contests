/**
 *    Created:
 *    Time Limit:
 *    Memory Limit:
 **/

#include <bits/stdc++.h>
using namespace std;

#define fastio()                 \
    ios::sync_with_stdio(false); \
    cin.exceptions(cin.failbit); \
    cin.tie(nullptr);            \
    cout.tie(nullptr);

using ll = long long;
using ull = unsigned long long;
using vl = vector<ll>;
using vvl = vector<vl>;
using vi = vector<int>;
using vvi = vector<vi>;

void solve() {}

int main() {
    fastio();

    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
